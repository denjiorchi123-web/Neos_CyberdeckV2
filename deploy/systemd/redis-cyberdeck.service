[Unit]
Description=CyberDeck Redis
After=network.target

[Service]
Type=notify
ExecStart=/usr/bin/redis-server --bind 0.0.0.0 --port 6379 --save "" --appendonly no
Restart=on-failure
User=redis

[Install]
WantedBy=multi-user.target
