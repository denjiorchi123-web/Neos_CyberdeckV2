# systemd-networkd: usb0 (USB-C gadget ethernet — management access).
# Static link-local /30 so an attached laptop gets the other half via DHCP.
[Match]
Name=usb0

[Network]
Address=169.254.10.1/30
DHCPServer=yes
IPMasquerade=no
LLMNR=no
MulticastDNS=yes
